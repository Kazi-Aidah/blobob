todo:
- fix radiuses
- fix keyboard button in hotkeys
- fix increased border radius in sidetabs
- hide help icon toggle
- exclude global radius & corner-shape affecting checkboxes
- fix padding of centered statusbar when bubble layout is in use.
- fix hide strikethrough (when custom checkboxes disabled)
- disable animations on mobile
- make border radius work for properties

fun:
```css
.nav-folder {
    /* border: 2px solid red; */
    background-color: var(--background-primary);
    border-radius: var(--nav-folder-radius);
    padding: 10px;
}
```